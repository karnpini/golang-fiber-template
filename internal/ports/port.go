package ports

// Example port interface
type HandlerPort interface {
	// GetContentHandler(c *fiber.Ctx) error
}

// type ServicePort interface {
// 	GetContent(contentId string, lang string, version string) (map[string]interface{}, error)
// }

// type ClientPort interface {
// 	GetContent(contentId string, lang string, version string) ([]byte, error)
// }
